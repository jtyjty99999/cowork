# 测试指南

## 🧪 验证聊天触发两次的修复

### 测试步骤

1. **打开浏览器开发者工具**
   - 按 `F12` 或右键 → 检查
   - 切换到 Console 标签

2. **刷新页面**
   - 按 `Cmd+R` (Mac) 或 `Ctrl+R` (Windows)
   - 观察初始消息是否只显示一次

3. **发送测试消息**
   ```
   测试消息：find my files
   ```
   - 在输入框输入上述消息
   - 按 Enter 发送
   - 观察是否只有一条用户消息和一条 AI 响应

4. **检查控制台**
   - 不应该看到重复的日志
   - 不应该看到相同的消息出现两次

### 预期结果

✅ **正确行为**：
- 初始化消息只显示一次
- 每次发送消息只触发一次 AI 响应
- 消息列表中没有重复项

❌ **错误行为**（已修复）：
- 初始化消息显示两次
- 发送一条消息收到两条相同的 AI 响应
- 控制台显示重复的日志

## 🎯 功能测试清单

### 基础功能

- [ ] 创建新任务
- [ ] 切换任务
- [ ] 编辑任务标题
- [ ] 发送消息
- [ ] 接收 AI 响应

### 特殊关键词测试

测试以下关键词，确认 AI 响应正确：

1. **文件搜索**
   ```
   find my drafts
   search for files
   ```
   预期：显示文件搜索命令和工作文件列表

2. **文件整理**
   ```
   organize my files
   sort documents
   ```
   预期：显示整理建议

3. **创建文件**
   ```
   create an artifact
   make a new file
   ```
   预期：在 Artifacts 面板显示新文件

4. **通用对话**
   ```
   hello
   help me
   ```
   预期：显示通用响应

### UI 测试

- [ ] 左侧边栏任务列表正常显示
- [ ] 聊天区域消息正常滚动
- [ ] 右侧边栏进度正常更新
- [ ] Artifacts 列表正常显示
- [ ] Working files 列表正常显示
- [ ] 输入框自动调整高度
- [ ] 模型选择器可以切换

### 性能测试

- [ ] 页面加载速度正常
- [ ] 消息发送响应及时
- [ ] 没有明显的卡顿
- [ ] 内存使用正常

## 🐛 已知问题

目前没有已知问题。如果发现新问题，请记录在这里。

## 📊 测试报告模板

```
测试日期：____
测试人员：____
浏览器：____ 版本：____

测试结果：
✅ 通过 / ❌ 失败

问题描述（如有）：
____

重现步骤：
1. ____
2. ____
3. ____

预期结果：____
实际结果：____
```

## 🔍 调试技巧

### 查看 React 组件渲染

在浏览器控制台输入：
```javascript
// 查看当前状态
window.__NEXT_DATA__
```

### 监控状态变化

在 `hooks/useCowork.ts` 中添加：
```typescript
useEffect(() => {
  console.log('State updated:', state);
}, [state]);
```

### 检查消息重复

在 `addMessage` 函数中添加：
```typescript
console.log('Adding message:', message);
```

## 💡 提示

- 在开发环境中，React StrictMode 会导致组件渲染两次，这是正常的
- 我们的修复确保了即使在 StrictMode 下，副作用也只执行一次
- 生产构建（`npm run build`）不会有 StrictMode 的双重渲染
